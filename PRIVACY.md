# Privacy

The extension sends requests to the machine-scoped Swobu endpoint. Request and response content exists transiently in extension memory for conversion and streaming; it is not persisted or logged by this extension. The extension has no independent telemetry uploader and never reads provider credentials.
