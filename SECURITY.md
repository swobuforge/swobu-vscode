# Security

Report vulnerabilities privately through GitHub Security Advisories. Do not include live credentials or prompt content. Executable, endpoint, and capability overrides are machine-scoped; workspace content cannot set them. Runtime commands use argument arrays without a shell.
