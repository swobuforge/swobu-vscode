import assert from "node:assert/strict";import test from "node:test";import * as vscode from "vscode";import {daemonAddress,executable,parseEndpoint} from "../src/settings.js";
test("V1 accepts only exact loopback origins",()=>{assert.equal(daemonAddress(parseEndpoint("http://127.0.0.1:7940")),"127.0.0.1:7940");assert.equal(daemonAddress(parseEndpoint("http://[::1]:7940")),"[::1]:7940");for(const value of ["https://example.com","http://192.168.1.2:7926","http://localhost:7926/path","http://user:secret@localhost:7926","http://localhost:7926?token=secret","file:///tmp/x"])assert.throws(()=>parseEndpoint(value));assert.throws(()=>parseEndpoint("not a URL"),/Swobu endpoint must be a valid URL/);});
test("configured executables must be absolute host paths",()=>{const setConfiguration=(vscode as unknown as {__setConfiguration:(key:string,value:unknown)=>void}).__setConfiguration;try{setConfiguration("executable","workspace/swobu");assert.throws(()=>executable(),/absolute path/);setConfiguration("executable","/opt/swobu/bin/swobu");assert.equal(executable(),"/opt/swobu/bin/swobu");}finally{setConfiguration("executable",undefined);}});
